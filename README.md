# Django REST Framework CRUD API (Practice Project)

This project is a **CRUD (Create, Read, Update, Delete) REST API** built using **Django Rest Framework (DRF)**.  
It was created as a practice project to understand API development, serializers, database integration, and HTTP methods.

---

## 🚀 Features

- GET – Fetch all users / single user
- POST – Create a new user
- PUT – Update an existing user
- DELETE – Delete a user
- Uses Django ORM with database
- Uses DRF serializers
- Supports DRF Browsable API
- Clean function-based views using `@api_view`

---

## 🛠️ Tech Stack

- Python 3
- Django
- Django Rest Framework
- SQLite (default Django DB)

---

## 📁 Project Structure

